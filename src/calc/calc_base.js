function Add(a, b) {
    if (a.constructor == Number && b.constructor == Number) {
        return a + b;
    }
}

function Sub(a, b) {
    if (a.constructor == Number && b.constructor == Number) {
        return a - b;
    }
}

function Mul(a, b) {
    if (a.constructor == Number && b.constructor == Number) {
        return a * b;
    }
}

function Div(a, b) {
    if (a.constructor == Number && b.constructor == Number) {
        return a / b;
    }
}

function Mod(a, b) {
    if (a.constructor == Number && b.constructor == Number) {
        return a % b;
    }
}

function Pow(a, b) {
    if (a.constructor == Number && b.constructor == Number) {
        return a ** b;
    }
}